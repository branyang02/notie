{
  "builds": [
    {
      "src": "demo-app/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "demo-app/dist",
        "buildCommand": "npm run build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}